import styles from './AddressSetting.module.css'

function AddressSetting() {
    return (  
        <div className={`${styles.wrapper}`}>
            <span className={`${styles.title}`}>Your address list</span>
            
        </div>
    );
}

export default AddressSetting;